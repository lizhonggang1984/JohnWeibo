<p id="notice"><%= notice %></p>

<h1>Admins</h1>

<table>
  <thead>
    <tr>
      <th>userid</th>
      <th>username</th>
      <th>email</th>
    </tr>
  </thead>

  <tbody>
    <% @admin.each do |admin| %>
      <tr>
	<td><%= admin.id %></td>
        <td><%= admin.username %></td>
        <td><%= admin.email %></td>
      </tr>
    <% end %>
  </tbody>

</table>

<br>
<%= link_to 'Back', admins_path %>

<br>
